## Advent of code 2026

Using C# this year, dotnet version 10.

To run a solution, go to the day folder and run `dotnet run {file}` where file is the path to the puzzle input file.
Defaults to `test.txt`
